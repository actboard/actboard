{
  "name": "actboard-example",
  "version": "1.0.0",
  "description": "Example Playwright project using @actboard/playwright-reporter",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@actboard/playwright-reporter": "^1.0.5",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.2"
  }
}
